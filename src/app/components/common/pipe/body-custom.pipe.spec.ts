import { BodyCustomPipe } from './body-custom.pipe';

describe('BodyCustomPipe', () => {
  it('create an instance', () => {
    const pipe = new BodyCustomPipe();
    expect(pipe).toBeTruthy();
  });
});
